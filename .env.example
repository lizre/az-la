# Copy this file to .env for local development (the .env file is gitignored so it stays off GitHub).
# In Azure, DATABASE_URL is set as an environment variable on the Container App directly.

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/learningdb
