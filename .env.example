# Local development only — never commit this to git!
# In Azure, these are set via App Service > Configuration > App Settings
# and the value comes from Key Vault using a Key Vault reference.

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/learningdb
